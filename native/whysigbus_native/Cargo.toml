[package]
name = "whysigbus_native"
version = "0.1.0"
authors = []
edition = "2018"

[lib]
name = "whysigbus_native"
path = "src/lib.rs"
crate-type = ["dylib"]

[dependencies]
rustler = {git = "https://github.com/rusterlium/rustler.git", rev = "96e6721"}
lazy_static = "1.0"
